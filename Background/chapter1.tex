\chapter{Background}
\label{chap:Multicat}
\section{Multicategories}
\label{sec:multicat}
{\bf EXPLAIN THE SOURCE OF THIS MATERIAL. ``Multicategories were first defined in ...'' ``Our exposition follows ...''}
yes
Multicategories are a generalization of categories. In 
categories, arrows have a single source/domain and a single target/codoamin. In multicategories, the arrows have a finite sequence of sources/inputs and a single target/output. 
\begin{defn}
    A \emph{multicategory} $\M$ consist of a set $S$ of objects and for each $n\geq 0$ and each sequence $s_1,\ldots, s_n,t$ of objects a set
    \[\M(s_1,\ldots,s_n;t) \] 
    of multiarrows/operations, thought of as taking $n$ inputs objects $s_1,\ldots,s_n$ respectively to the output object $t$. Moreover, there are three kinds of structure maps:
    \begin{itemize}
        \item For each object $s$, an identity/unit $1_s\in \M(s;s)$
        \item For any sequence of objects $s_1,\ldots,s_n,t$ and any $n$-tuple of sequences $d^i_1,\ldots,d^i_{k_i}$ of objects for $i=1,\ldots,n$, a composition function of multiarrows/operations 
        \[
        \gamma:\M(s_1,\ldots,s_n;t)\times\prod_{i=1}^{n}\M(d^i_1,\ldots,d^i_{k_i};s_i)\rightarrow\M(d^1_1,\ldots,d^1_{k_1},d^2_1,\ldots,d^n_{k_n};t),
        \]
        usually written $p\circ(q_1,\ldots,q_n):=\gamma(p,q_1,\ldots,q_n)$ or even just $p(q_1,\ldots,q_n)$
    \end{itemize}
    Furthermore these structure maps have to satisfy a number of axioms, which express that composition is unital and associative \citep{HM}
    % {\bf ADD SPECIFIC CITATION WHERE THE FULL DEFINITION APPEARS.}
\end{defn}

\begin{rmk}
Let $\M$ be a multicategory, $f\in\M(s_1,\ldots,s_n;t)$ and $g\in\M(d_1,\ldots,d_l;s_i)$ for some $1 \leq i \leq n$. It will be convenient to have the following notation:
\[
\iof{f}{g} := f(1_{s_1},\ldots,g,\ldots,1_{s_n})
\]
Thus, $\iof{f}{g}$ is the composition of $f$ with $g$ in the $i$th variable.
\end{rmk}

\begin{ex}
    You can think of $n$-ary multiarrows in $\M(s_1,\ldots, s_n;t)$ as n-ary operations. Let $M$ be the multicategory with a single object $X$. Then the $n$-ary multiarrows in $M(X,\dots,X;X)$define to be the set of functions 
    \[
    f:X\times \cdots \times X \rightarrow X 
    \]
    where the domain is the n-fold cartesian product of $X$ with itself.
\end{ex}
\begin{ex} 
    We can up the level from the previous example, by fixing a family of sets $ \{X_c\}_{c\in C}$ indexed by a set of objects $C$. Then you can think of the multiarrows in $\M(c_1,\ldots,c_n;c)$ as n-ary operations, taking inputs of types $\lst{c}{n}$ respectively to an output of type $c$.

    For example, for a fixed family of sets $\{X_c\}_{c\in C}$, define a multicategory $\M$ whose objects is the set of colors $C$,  and whose multiarrows are defined by declaring $\M(c_1,\ldots, c_n;c)$ to be the set of functions 
     \[
     p:X_{c_1}\times \cdots \times X_{c_n}\rightarrow X_c
    \]
    Composition in $\M$ is defined as composition of functions.
\end{ex}

% {\bf FIX A SINGLE SET $X$? and then $n$-ary multiarrows are operations on $X$.}

\begin{ex}
    The previous two examples are specific instances of a more general family of examples example. If $\C$ is a symmetric monoidal category and $C$ is a set of objects of $\C$, then one obtains a multicategory with this set of objects by defining $\M(c_1,\ldots,c_m;t)$ to be the set of morphisms $c_1\otimes \dots \otimes c_n \rightarrow t$ in $\C$. We will sometimes write $\C^{\otimes}$ for the multicategory obtained in this way, when $C$ is the set of all objects of $\C$ (granted it is small).
\end{ex}
\begin{ex}
    A (small) category $\C$ can be considered as a multicategory $\C_m$ which only have unary arrows. I.e. the set $\C_m(c_1,\ldots,c_n;c)$ is always empty unless $n=1$.
\end{ex}

We can observe that multicategories form a category $\MCat$.


\begin{defn}
For two multicategories $\M$ and $\mathcal{N}$, a \emph{multifunctor} $\phi:\M\rightarrow\mathcal{N}$ consist of:
\begin{itemize}
    \item A function $f:O_{\M}\rightarrow O_{\mathcal{N}}$ between the corresponding sets of objects
    \item For each sequence $s_1,\ldots,s_n,t$ of objects of $\M$, a map 
    \[
        \phi_{\lst{s}{n},t}:\M(\lst{s}{n};t)\rightarrow \mathcal{N}(\flst{s}{n}{f};f(t)).
    \]
\end{itemize}
So that these maps are compatible with compositions and units in the obvious way.
\end{defn}
\subsection{The Tensor Product of Multicategories}
\label{sec:The Tensor Product of Multicategories}

Let $M$ and $N$ be multicategories. Their Boardman-Vogt tensor product $M \bv N$ is a multicategory defined to make sense of what a $M$-algebra in the category of $N$-algebras is and viceversa. We will not talk about the algebras of a multicategories here, but we refer the reader to [look for reference] % citep{?} and \citep{?} %} for more details.
\begin{defn}
    The \emph{Boardman-Vogt tensor} $M \bv N$ in terms of generators and relations. The set of objects of this multicategory is the product of the sets of objects of $M$ and $N$; if $x$ and $y$ are objects of $M$ and $N$ respectively, we write $x\otimes y$ for the corresponding object of $M \bv N$. The generating multiarrows of $M \bv N$ are of two types:
\begin{itemize}
    \item For each multiarrow $f \in M (\lst{x}{n},x)$ and object $y$ of $N$, a multiarrow \[
    f \otimes y \in (M\bv N)(\tflst{x}{n}{y};x\otimes y)
    \]
    \item For each object $x$ of $M$ and multiarrow $g \in N(\lst{y}{m};y)$, a multiarrow
    \[
    x \otimes g \in (M\bv N)(\lst{x\otimes y}{m}; x\otimes y)
    \]
\end{itemize}
The relations to be satisfied by these generators are the following:
\begin{enumerate}[label = (\arabic*)]
    \item $(f\otimes y)(\tflst{f}{n}{y}) = f(\lst{f}{n})\otimes y$
    \item $(x\otimes g)(\lst{x\otimes g}{m})= x \otimes g(\lst{g}{m})$
    \item $\sigma^*(f \otimes y) = (\sigma^* f)\otimes y$ for $\sigma \in \Sigma_n$
    \item $\sigma^*(x \otimes g) = x \otimes (\sigma^* g)$ for $\sigma \in \Sigma_m$
    \item $(f \otimes y)(\tflst{x}{n}{g}) = \sigma^*_{n,m}((x \otimes g)(\lst{f\otimes y}{m}))$
\end{enumerate}
\end{defn}

Note that relations (1) and (2) say precisely the condition that for any object $y$ of $N$ there is a multifunction $-\otimes y:M \rightarrow M \bv N$. Similarly, for any object $x$ of $M$ there is a multifunction $x \otimes -:N \rightarrow M \bv N$, this time given by relations (2) and (4). Relation (5) is the most interesting; most often refered to it as the \emph{Boardman-Vogt interchange relation}. The permutation $\sigma_{n,m} \in \Sigma_{nm}$ is the one that makes sense of formula (5), i.e. the element relating the sequences $(\lst{x_1 \otimes y}{m},\dots,\lst{x_n \otimes y}{m})$ and $(\tflst{x}{n}{d_1},\dots,\tflst{x}{n}{d_m})$.

\subsection{Trees}
\label{sec:trees}
We will do a quick detour to talk about our main combinatorial object that is gonna help us with studying multicategories, trees. We are mainly interested in finite rooted tree. The main difference with acyclic graphs from graph theory is edges are not required to have two vertices on either end, and we have a natural partial order to the set of edges and vertices. 
\begin{defn}
    A \emph{tree} $T$ consist of a finite set $V$ of \emph{vertices}, a nonempty finite set $E$ of \emph{edges}, a distinguished element $r\in E$ called the \emph{root}, together with the following data:
    \begin{enumerate}[label= (\arabic*)]
        \item a function $I:E-\{r\}\rightarrow V$, which we think of as assigning to an edge $e$ the vertex $I(e)$ of which is an input. Gramatically, we say "$e$ is an input edge of $f$, iff $I(e)=f$"
        \item A function $O:V\rightarrow E$, assigning to each vertex $g$ its output edge $O(g)$. Gramatically, we say "$x$ is the output edge of $f$ iff $O(f)=x$"
    \end{enumerate}
For each edge $e$ other than the root $r$, we obtain a sequence of edges starting at $e$ by repeatedly applying $O\circ I$. We demand that this sequence ends in the root $r$ after finitely many steps, for an arbitrary starting edge.
\end{defn}

\begin{ex}
    A typical tree $T$ with $E(T)=\{x,y,z,w,v,r\}$ and $V(T)=\{f,g,h,l\}$ could look like:
    \begin{center}
\begin{tikzpicture}[
  vertex/.style={circle, fill=black, inner sep=1.5pt},
  edge/.style={draw},
  lab/.style={font=\small}
]

% vertices (explicit coordinates)
\node[vertex, label=above:$f$] (f) at (-.2,0) {};
\node[vertex, label=right:$g$] (g) at (-.7,-1.4) {};
\node[vertex, label=left:$l$] (l) at (.6,-3.0) {};
\node[vertex, label=above:$h$] (h) at (.6,-1.4) {};

% dangling endpoints (no vertices)
\coordinate (xend) at (-1.3,-.2);
\coordinate (wend) at (1.9,-1.5);
\coordinate (rend) at (0.6,-4.6);

% edges
\draw[edge] (xend) -- node[lab, left, pos = .45] {$x$} (g);
\draw[edge] (f) -- node[lab, right] {$y$} (g);

\draw[edge] (g) -- node[lab, left] {$v$} (l);
\draw[edge] (h) -- node[lab, right] {$z$} (l);

\draw[edge] (l) -- node[lab, right ] {$w$} (wend);
\draw[edge] (l) -- node[lab, left] {$r$} (rend);

\end{tikzpicture}
\end{center}
We can fully define both functions for this small example as:
\begin{align*}
    I(x)=I(y)=g,\ \ I(v)=I(z)=I(w)=w\ \\
    O(f)=y,\ \ O(g)=v,\ \ O(h)=z,\ \ O(l)=r    
\end{align*}


With the graphical representation of a tree, we can see that the function $I$ assigns each edge to the vertex right below it. As in:
\end{ex}

<<<<<<< Updated upstream
 The edges in the complement of the image of $O$ are called \emph{leaves} of the tree $T$. The vertices in the complement of the image of $I$ are called \emph{stumps}, or \emph{nullary vertices.} An \emph{outer edge} is an edge tha is either the root or a leaf. An \emph{inner edge} is any other edge of $T$. Such an edge is both an output edge and an input edge for some other vertex. The vertices come in two kinds, \emph{external vertices} which only have one inner edge attached to them, and \emph{internal vertices} 
When writing $T$ for a tree, we will usually write $E(T)$ and $V(T)$ for its sets of edges and vertices, repectively.
\newpage
\begin{ex}\label{ex:mu}
    The smallest possible tree consiste of a single edge and no vertices; this tree will be denoted by $\mu$
    % https://q.uiver.app/#q=WzAsMyxbMCwxLCJcXGJ1bGxldCJdLFswLDJdLFswLDBdLFsyLDEsIiIsMCx7InNob3J0ZW4iOnsic291cmNlIjo1MH0sInN0eWxlIjp7ImhlYWQiOnsibmFtZSI6Im5vbmUifX19XV0=
\[\begin{tikzcd}
	{} \\
	 \\
	{}
	\arrow[between={0.5}{1}, no head, from=1-1, to=3-1]
\end{tikzcd}\]
\end{ex}
\begin{ex}
The next smallest tree consist of a single edge and single vertex, pictured as:
% https://q.uiver.app/#q=WzAsMyxbMCwxLCJcXGJ1bGxldCJdLFswLDJdLFswLDBdLFsyLDEsIiIsMCx7InNob3J0ZW4iOnsic291cmNlIjo1MH0sInN0eWxlIjp7ImhlYWQiOnsibmFtZSI6Im5vbmUifX19XV0=
\[\begin{tikzcd}
	{} \\
	\bullet \\
	{}
	\arrow[between={0.5}{1}, no head, from=1-1, to=3-1]
\end{tikzcd}\]
\end{ex}


We will always pictures trees with the root at the bottom and the leaves at the top. The valence of a vertex $v$ is the number of input edges it has, i.e.\ the cardinality of the set $I^{-1}(v)$. A vertex of valence $0$ is what we called a \emph{nullary vertex} or a \emph{stump}. The sets $E(T)$ and $V(T)$ both have a natural partial ordering, where $x < y$ for edges $x$ and $y$ (or $f < g$ for vertices $f$ and $g$) if there is a sequence of edges starting at $x$ (resp. $f$) and ending at the root of T, $r$, and passes through $y$ (resp. $g$).
=======
We will always pictures trees with the root at the bottom and the leaves at the top. We will be extensively using the natural partial ordering on the set of edges. This partial order on the edges can be used to define \emph{incoming edges} and \emph{outgoing edges} for any vertex $f$, which we will denote by $in(f)$ and $out(f)$, respectively. The valence of a vertex $v$ is the number of input edges it has, i.e.\ the cardinality of the set $I^{-1}(v)$. A vertex of valence $0$ is what we called a \emph{nullary vertex} or a \emph{stump}. The sets $E(T)$ and $V(T)$ both have a natural partial ordering, where $x < y$ for edges $x$ and $y$ (or $f < g$ for vertices $f$ and $g$) if there is a sequence of edges starting at $x$ (resp. $f$) and ending at the root of T, $r$, and passes through $y$ (resp. $g$).
>>>>>>> Stashed changes

% {\bf I don't if i need this but ill add it for now}

% \begin{defn}
%     If $T$ is a tree, then a \emph{planar structure} on $T$ is a linear ordering on the set of input edges of every vertex of $T$.
% \end{defn}
% The multicategory $\Omega(T)$ is an example of a free multicategory, in the sense we will make precise in the next section.
% \label{sec:Free Multicategories}

We note that removing an external vertex and all the outer edges attached to it yields a new tree. We will refer to this procedura as \emph{pruning}. 

% We will be extensively using the natural partial ordering on the set of edges. We will call two edges $x$ and $y$ \emph{incomparable} if they are not related in this partial orde
%  We will remember our first example of a tree, referred to as $\mu$ (Example \ref{ex:mu}). This tree will play a special role in what follows. For now we will just say that is has an unique edge which is both a lead and a root.
\begin{defn}
 A tree is called \emph{open} if it has no nullary vertices; on the other side, a tree is called \emph{closed} if it has no leaves.   
\end{defn}
\begin{defn}
    A tree is called \emph{linear} if all its vertives have valence one.
\end{defn}
  As an example, the tree $\mu$ is open but not closed, and generally a tree cannot be both open and closed.  A linear tree with $n+1$ edges running from the leaf 0 to the root $n$ will be denoted by $\left[n\right]$:
 \begin{center}
    
    \begin{tikzpicture}[
    vertex/.style={circle, fill=black, inner sep=1.5pt},
    edge/.style={draw},
    lab/.style={font=\small}
    ]
    \node[vertex] (f) at (0,1) {};
    \node[vertex] (g) at (0,-1.2) {};
    \node (center) at (0,0) {$\vdots$};

    % dangling endpoints (no vertices)
    \coordinate (fup) at (0,1.5);
    \coordinate (gdown) at (0,-1.7);
    \coordinate (fdown) at (0,.5);
    \coordinate (gup) at (0,-.7);

    % edges
    \draw[edge] (fup) -- node[lab, left] {$0$} (f);
    \draw[edge] (f) -- node[lab, left] {$1$} (fdown);

    \draw[edge] (gup) -- (g);
    \draw[edge] (g) -- node[lab, left] {$n$} (gdown);
    \end{tikzpicture}

\end{center}

The reason for this notation is that the linear trees will correspond to the standard simplices in the tree category $\Omega$. Note that by our convention the tree obtained from $[n]$ by puttting a stump on top will not be refferred to as a linear tree. 

Another family of trees that we will become familiar with are the \emph{corollas}, which are trees with a single vertex. The corolla with $n$ leaves labeled $1,\dots n$ and root $0$ will be denoted by $C_n$:
\begin{center}    
    \begin{tikzpicture}[
        vertex/.style={circle, fill=black, inner sep=1.5pt},
        edge/.style={draw},
        lab/.style={font=\small}
        ]
        \node[vertex] (c0) at (-5,0) {};
        \node[vertex] (c1) at (-2.5,0) {};
        \node[vertex] (c2) at (0,0) {};
        \node[vertex] (c3) at (2.5,0) {};
        \node (center) at (5,0) {$\cdots$};

        \coordinate (c0down) at (-5,-1);
        \coordinate (c1down) at (-2.5,-1);
        \coordinate (c2down) at (0,-1);
        \coordinate (c3down) at (2.5,-1);

        \coordinate (c1up) at (-2.5,1);

        \coordinate (c2upl) at (-.4,1);
        \coordinate (c2upr) at (.4,1);

        \coordinate (c3upl) at (2.1,1);
        \coordinate (c3up) at (2.5,1);
        \coordinate (c3upr) at (2.9,1);

        \draw[edge] (c0down) -- node[lab, right, pos=.1] {$\hspace{.5em} C_0$} (c0);
        \draw[edge] (c1down) -- node[lab, right, pos=.1] {$\hspace{.5em} C_1$} (c1);
        \draw[edge] (c2down) -- node[lab, right, pos=.1] {$\hspace{.5em} C_2$} (c2);
        \draw[edge] (c3down) -- node[lab, right, pos=.1] {$\hspace{.5em} C_3$} (c3);

        \draw[edge] (c1up) -- (c1);
        \draw[edge] (c2upl) -- (c2);
        \draw[edge] (c2upr) -- (c2);
        \draw[edge] (c3upl) -- (c3);
        \draw[edge] (c3up) -- (c3);
        \draw[edge] (c3upr) -- (c3);
    \end{tikzpicture}
\end{center}
\begin{defn}
    An \emph{embedding} $\phi:S\rightarrow T$ of trees consists of injective maps $\phi_V:V(S)\rightarrow V(T)$ and $\phi_E:E(S)\rightarrow E(T)$ such that for any vertex $f$ of $S$, the map $\phi_E$ gives a bijection between $in(f)$ and $in(\phi_V(f))$, and $phi_E(O(f))=O(\phi_V(f))$
\end{defn} 
A tree $T$ defines a multicategory $\Omega(T)$ as follows: 
\begin{itemize}
    \item The objects of $\Omega(T)$ are the edges of $T$.
    \item An operation/multiarrow in $\Omega(T)(e_1,\ldots,e_n;e)$ is a vertex $v$ of $T$ such that the input edges of $v$ are exactly $e_1,\ldots,e_n$ and the output edge of $v$ is $e$. Note that for any sequence of colors $e_1,\dots,e_2,e$ there is at most one operation/multiarrow in $\Omega(T)(e_1,\ldots,e_n;e)$.
    \item Composition is given by the tree structure in the obvious way. In more detailed, is the operation/multiarrow given by squashing an inner edge and identifying the two vertices to the composition of the two corresponding operations/multiarrows.
\end{itemize}

\subsection{Dendroidal Sets}
\label{sec:dset}

Dendroidal sets are a generalization of simplicial sets that are particularly well suited to study operads and multicategories. Just as simplicial sets are presheaves on the simplex category $\Delta$, dendroidal sets are presheaves on the dendroidal category $\Omega$.
\begin{defn}
    The category $\Omega$ consist of:
    \begin{enumerate}
        \item Objects: finite rooted trees
        \item Morphisms: for two trees $S$ and $T$, a morphism from $S$ to $T$ is a multifunctor $\Omega(S)\rightarrow \Omega(T)$ between the corresponding multicategories.
    \end{enumerate}
    Another way to say this is that $\Omega$ is the full subcategory of $\MCat$ spanned by the multicategories of the form $\Omega(T)$ for some tree $T$.
\end{defn}
 
    % \begin{thm}
% Something Ramdon

% \end{thm}

% \begin{defn}
% definition now. Here is $\Set$
% \end{defn}

% \begin{rmk}
%     here goes it
% \end{rmk}